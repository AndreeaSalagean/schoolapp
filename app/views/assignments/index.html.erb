<%= render 'chapters/chapter' %>
    
 <div class="col-sm-9">
   <h4><small>ASSIGNMENTS</small></h4>
      <hr><p>
      
  <% @assisgnments.each do |assignment| %>

    <ul class="list-group">
      
        <dt><li class="list-group-item-active"><%= link_to assignment.title, course_chapter_assignment_path(chapter_id: @chapter.id, course_id: @course.id) ,style: 'color:#FFFFFF;', class: 'css_class'%></li></dt>
      
    </ul>
 <% end %>
